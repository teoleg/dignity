# Real order forms and proofs contain a named family's personal details and
# vendor paperwork. They are reference material only and must never be
# committed. Keep them local; see samples/README.md.
samples/*
!samples/README.md

node_modules/
dist/

# Build output and the vendor blank, which is not ours to publish.
public/app.js
public/blank-form.jpg
.next/
coverage/

.DS_Store
*.tmp
